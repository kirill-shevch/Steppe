using UnityEngine;

namespace Steppe.Caravan
{
    /// <summary>
    /// Marker used by first-person build raycasts. Only marked deck colliders can
    /// receive a module preview.
    /// </summary>
    public sealed class CaravanBuildSurface : MonoBehaviour
    {
    }
}
